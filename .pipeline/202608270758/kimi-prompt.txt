Проект: So to Speak (FunnyEnglish) — Kotlin Multiplatform (модуль composeApp), рабочий каталог — корень репозитория (ветка develop, рабочее дерево содержит незакоммиченные изменения предыдущего прогона — их НЕ трогать). Ты выполняешь follow-up задачу bd FunnyEnglish-4d1: унификация Ktor 3.0.3 + явный движок медиа-клиента.

Выполни ТОЛЬКО следующие правки (больше ничего не меняй, не удаляй и не «улучшай»):

1. gradle/libs.versions.toml: в секции [versions] строка ktor = "3.0.2" → ktor = "3.0.3". Больше в каталоге ничего не менять.

2. composeApp/src/commonMain/kotlin/com/sotospeak/app/player/MediaHttpClient.kt: сделать выбор HTTP-движка явным через expect/actual фабрику (документированный Ktor-KMP-шаблон):
   - добавить internal expect fun createPlatformMediaHttpClient(config: HttpClientConfig<*>.() -> Unit): HttpClient
   - изменить create() на: fun create(): HttpClient = createPlatformMediaHttpClient { mediaConfig() }
   - mediaConfig() (internal) и константы CONNECT_TIMEOUT_MS / INFINITE_TIMEOUT_MS НЕ менять; KDoc дополнить (движок теперь явный: OkHttp на android/desktop, Js на wasmJs, Darwin неявно на ios).

3. Создать по одному actual-файлу в каждом платформенном source set (пакет com.sotospeak.app.player, файл MediaHttpClient.<platform>.kt):
   - composeApp/src/androidMain/kotlin/com/sotospeak/app/player/MediaHttpClient.android.kt: internal actual fun createPlatformMediaHttpClient(config: HttpClientConfig<*>.() -> Unit): HttpClient = HttpClient(OkHttp) { config(this) } (импорт io.ktor.client.engine.okhttp.OkHttp)
   - composeApp/src/desktopMain/kotlin/com/sotospeak/app/player/MediaHttpClient.desktop.kt: то же самое (OkHttp)
   - composeApp/src/wasmJsMain/kotlin/com/sotospeak/app/player/MediaHttpClient.wasmJs.kt: internal actual fun createPlatformMediaHttpClient(config: HttpClientConfig<*>.() -> Unit): HttpClient = HttpClient(Js) { config(this) } (импорт io.ktor.client.engine.js.Js)
   - composeApp/src/iosMain/kotlin/com/sotospeak/app/player/MediaHttpClient.ios.kt: internal actual fun createPlatformMediaHttpClient(config: HttpClientConfig<*>.() -> Unit): HttpClient = HttpClient { config(this) } — движок НЕ указывать (Darwin — единственный движок на runtime, compile-зависимость в composeApp не добавлять)

4. Создать тест composeApp/src/desktopTest/kotlin/com/sotospeak/app/tests/MediaHttpClientEngineTest.kt:
   - класс MediaHttpClientEngineTest, kotlin.test; единственный @Test: val client = MediaHttpClient.create() → assertTrue(client.engine is OkHttpEngine, "движок медиа-клиента должен быть OkHttpEngine (явный, не ServiceLoader)") с импортом io.ktor.client.engine.okhttp.OkHttpEngine → finally client.close(). Сетевых запросов нет. KDoc: детерминированный движок (bd 4d1, D2).

Ограничения:
- НЕ меняй: AppModule.kt, VideoPlayerController.*, VideoScreen.kt, VideoScreenTest.kt, MediaHttpClientTest.kt, shared/, core/, feature-*, backend/, admin-web/.
- НЕ запускай gradle-сборки/тесты/линт (я прогоню гейты сам), НЕ делай git-коммитов/пушей.
- После правок выведи краткий отчёт: список изменённых/созданных файлов и суть изменений.