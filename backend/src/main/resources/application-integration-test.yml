# Integration test profile configuration
# Used for integration tests with PostgreSQL

spring:
  application:
    name: sotospeak-backend-integration-test

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:sotospeak_integration}
    username: ${DB_USERNAME:test}
    password: ${DB_PASSWORD:test}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    # OSIV off — как в prod (application.yml)
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_batch_fetch_size: 16

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    clean-disabled: false  # Allow clean for tests

server:
  port: 8080

app:
  jwt:
    # Минимум 32 байта — JwtService fail-fast (require >= 32 байт для HS256).
    secret: ${JWT_SECRET:integration-test-jwt-secret-key-0123456789}
    expiration: 86400000  # 24 hours

  s3:
    endpoint: ${S3_ENDPOINT:http://localhost:9000}
    public-url: ${S3_PUBLIC_URL:${S3_ENDPOINT:http://localhost:9000}}
    access-key: ${S3_ACCESS_KEY:minioadmin}
    secret-key: ${S3_SECRET_KEY:minioadmin}
    bucket: ${S3_BUCKET:sotospeak}
    region: ${S3_REGION:us-east-1}

  cors:
    allowed-origins: "*"

  admin:
    email: ${ADMIN_EMAIL:admin@sotospeak.com}
    password: ${ADMIN_PASSWORD:admin123}
    display-name: ${ADMIN_DISPLAY_NAME:TestAdmin}

logging:
  level:
    com.sotospeak: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
