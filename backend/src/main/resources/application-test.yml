# Test profile configuration
# Used for unit tests with H2 database

spring:
  application:
    name: sotospeak-backend-test

  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    # OSIV off — как в prod (application.yml): тесты обязаны ловить LazyInitializationException
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        default_batch_fetch_size: 16
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
      data-locations: classpath:data-h2.sql

  flyway:
    enabled: false

  h2:
    console:
      enabled: true

server:
  port: 8080

app:
  jwt:
    secret: test-jwt-secret-key-that-should-not-be-used-in-production-only-for-testing
    expiration: 3600000  # 1 hour for tests

  s3:
    endpoint: http://localhost:9000
    public-url: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin
    bucket: sotospeak-test
    region: us-east-1

  cors:
    allowed-origins: "*"

  admin:
    email: ${ADMIN_EMAIL:admin@sotospeak.com}
    password: ${ADMIN_PASSWORD:admin123}
    display-name: ${ADMIN_DISPLAY_NAME:TestAdmin}

logging:
  level:
    com.sotospeak: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
