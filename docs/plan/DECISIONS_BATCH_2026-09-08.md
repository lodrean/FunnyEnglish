# DECISIONS_BATCH_2026-09-08 — решения владельца (ADR-007)

> Читайте AI-агентом перед реализацией хвостов. Каждый вопрос требует явного
> ответа владельца (ADR-007 human-in-the-loop). После ответов — реализация
> по стандартному циклу: claim → реализация → гейты → живой прогон → close.
>
> **Формат ответа:** прямо в этом файле пометить `РЕШЕНИЕ (дата): …` как в
> DECISIONS_BATCH_2026-09.md, или ответом в bd-задаче.

Контекст: за сессии 2026-09-07/08 закрыто 17 задач + 3 эпика (qbq 9/9,
2oz 14/14, wy7 9/9). Оставшиеся 11 open — все из этого списка.

---

## 1. 5tf.4 — навигация этап 2 (navigation-compose 2.8) — P2

Этап 1 (AppBackStack, bd 5tf.3) уже решает прод-баги (process death,
системный «назад», WASM history-guard). Миграция на navigation-compose
требует правки спеки Part 2 §1.1/§1.2 (ADR-007) и несёт риски: 15+ экранов,
перенос transition-слоя, WASM history, перекалибровка e2e-cmp.

- **Вариант A:** мигрировать (typed routes, SavedStateHandle, deep links)
  через OpenSpec change.
- **Вариант B (рекомендация из аудита):** закрыть 5tf.4 как отложенный;
  deep links вернуть в бэклог после MVP.

РЕШЕНИЕ:

## 2. ku2 — tokens.json v1.3.2 + remediation хардкод-цветов — P3

Аудит выполнен (docs/ds/COLOR_AUDIT_2026-09-05.md), пиксельно-нейтральная
замена 38 вхождений сделана и верифицирована 156/156. Остаток — только после
одобрения владельцем v1.3.2: легаси-дрейф #4A90D9 (7), #E8EAF6 (17),
MUI-grey (29), Material-StatusBadge (3), ErrorBoundary (5), GradingDetail
(2), composeApp 7 hex, экспорт secondary-шкалы в генератор.

- **Вариант A (рекомендую):** одобрить v1.3.2 → расширение tokens.json →
  regen 4 артефактов → remediation по списку аудита → tsc/vitest/visual-гейт.
- **Вариант B:** отложить до отдельного ревью палитры.

РЕШЕНИЕ:

## 3. h3l.5 — self-scoring Training: модель «эталонного ответа» — P3

Требуется решение, ЧТО показывать ученику после попытки как эталон:

- **Вариант A (быстро, рекомендую как MVP):** эталон = транскрипт видео
  топика (WebVTT уже есть, ноль backend-работы; карточка после попытки).
- **Вариант B:** «настоящий» эталон — текст/видео от учителя: новое поле
  Topic.modelAnswer, UI в админке, миграция, правка спеки Part 1.
- **Вариант C:** отложить h3l.5 целиком.

РЕШЕНИЕ:

## 4. b85.6 — RU-унификация админки + перегенерация visual-базлайнов — P3

Перевод Dashboard/Users/Analytics/Login на русский снесёт: текст-чувствительные
E2E-ассерты и ВСЕ visual-базлайны Chromatic/visual-regression. Регенерация —
только через docker visual-regen pipeline (свежий стек -p visualregen,
mcr playwright v1.58.2, контрольный полный прогон — грабля №113).

- **Вариант A:** разрешить RU-унификацию + запустить visual-regen pipeline
  (прогон занимает время, выполняется под контролем владельца).
- **Вариант B:** отложить (EN-локаль админки остаётся).

РЕШЕНИЕ:

## 5. h3l.17 — push-напоминание цели дня (WorkManager) — P4

In-app вечерний баннер сделан (h3l.14). Push требует: androidx.work уже
введён (h3l.19), осталось POST_NOTIFICATIONS runtime-permission (API 33+),
время напоминания и UX-тексты.

- **Вариант A:** сделать (время по умолчанию — 19:00, отключаемо в настройках).
- **Вариант B (рекомендую пока):** отложить — in-app баннер уже покрывает
  «мягкое напоминание»; пуш-разрешения для P4-фичи избыточны до MVP.

РЕШЕНИЕ:

## 6. h3l.18 — транскодинг видео (остаток h3l.7) — P3

ffprobe-валидация сделана (d8dadae): неподдерживаемый кодек отсекается 400.
Транскодинг — автоматическое перекодирование вместо отказа:

- **Вариант A (рекомендую):** фоновая джоба (очередь в БД + ffmpeg-воркер,
  статус в админке) — админ не ждёт.
- **Вариант B:** синхронный транскод на загрузке (медленно, но проще).
- **Вариант C:** отложить — админ перекодирует вручную; 400-подсказка уже
  направляет.

РЕШЕНИЕ:

## 7. h3l.20 — перевод слов в карточке словаря — P4

Карточка слова сделана без перевода (h3l.13). Источник перевода требует
выбора (данные учеников уходят наружу → ADR):

- **Вариант A:** внешний переводной API (провайдер/ключи на выбор владельца;
  серверный прокси с кэшем).
- **Вариант B:** встроенный мини-словарь (ограниченное покрытие, без сети).
- **Вариант C (рекомендую пока):** отложить — сохранение слов уже работает.

РЕШЕНИЕ:

---

## b85.4 — вне пакета

Zod-валидация (in progress) — чужая незавершённая сессия; не трогается до
завершения её автора.
