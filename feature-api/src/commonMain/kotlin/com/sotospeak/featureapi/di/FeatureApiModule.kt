package com.sotospeak.featureapi.di

import org.koin.dsl.module

/**
 * DI module for feature API
 */
val featureApiModule = module {
    // Feature registry will be provided by the app module
    // after all features are registered
}
